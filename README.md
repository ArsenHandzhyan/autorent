# AutoRent - Система аренды автомобилей

**Версия:** v1.4.0  
**Дата обновления:** 2025-01-27

## 📋 Описание проекта

AutoRent - это веб-приложение для аренды автомобилей, построенное на Spring Boot с использованием Thymeleaf для фронтенда. Система предоставляет полный функционал для управления автопарком, бронирования автомобилей и обработки платежей. **Приложение полностью адаптировано для использования на мобильных устройствах, планшетах и десктопах.**

## ✨ Основные возможности

### Для пользователей:
- 🔐 Регистрация и авторизация с верификацией email
- 🚗 Просмотр каталога автомобилей с фильтрацией
- 📅 Бронирование автомобилей с выбором дат
- 💳 Система ежедневных платежей
- 📱 Личный кабинет с историей аренд
- ⭐ Система отзывов и рейтингов
- 📞 SMS-уведомления
- 📱 **Полная мобильная адаптивность для смартфонов и планшетов**
- 👆 **Оптимизированное сенсорное управление**

### Для администраторов:
- 📊 Панель управления с аналитикой
- 🚙 Управление автопарком (CRUD операции)
- 👥 Управление пользователями и их правами
- ✅ Подтверждение/отмена аренд
- 💰 Финансовая отчетность
- 🔔 Система уведомлений
- 📱 **Адаптивный интерфейс для всех устройств**

## 🏗️ Архитектура

### Backend (Spring Boot):
- **Слоистая архитектура:** Controller → Service → Repository → Entity
- **DTO паттерн** для передачи данных между слоями
- **Валидация** на всех уровнях (Entity, DTO, Controller)
- **Обработка исключений** с кастомными error responses
- **Логирование** всех бизнес-операций
- **Транзакционность** для критических операций

### Frontend (Thymeleaf + Bootstrap):
- **Семантическая разметка** HTML5
- **Bootstrap 5** для responsive дизайна
- **Thymeleaf фрагменты** для переиспользования компонентов
- **Формы с валидацией** на клиенте и сервере
- **AJAX запросы** для динамического обновления
- **Mobile-First подход** в дизайне
- **Оптимизация для сенсорного управления**

### База данных (MySQL):
- **Нормализация** данных до 3NF
- **Индексы** для оптимизации запросов
- **Внешние ключи** для целостности данных
- **Триггеры** для автоматических операций
- **Миграции** для версионирования схемы

## 🛠️ Технологический стек

### Backend:
- **Spring Boot 3.x** - основной фреймворк
- **Spring Security** - аутентификация и авторизация
- **Spring Data JPA** - работа с базой данных
- **Spring Mail** - отправка email уведомлений
- **JWT** - токены для API
- **Lombok** - уменьшение boilerplate кода
- **MySQL** - база данных
- **Hibernate** - ORM

### Frontend:
- **Thymeleaf** - серверный шаблонизатор
- **Bootstrap 5** - CSS фреймворк (адаптивный)
- **Font Awesome** - иконки
- **JavaScript (ES6+)** - клиентская логика
- **AJAX** - асинхронные запросы
- **Responsive Design** - адаптивность для всех устройств

### Инфраструктура:
- **Docker** - контейнеризация
- **Maven** - управление зависимостями
- **Git** - система контроля версий

## 📱 Мобильная адаптивность

### Поддерживаемые устройства:
- 📱 **Смартфоны** (320px - 768px)
- 📱 **Планшеты** (768px - 1024px)
- 💻 **Десктопы** (1024px+)

### Особенности мобильной версии:
- **Mobile-First дизайн** - разработка начинается с мобильных устройств
- **Сенсорная оптимизация** - кнопки размером минимум 44x44px
- **Адаптивная типографика** - читаемый текст на всех экранах
- **Оптимизированные изображения** - быстрая загрузка на медленных соединениях
- **Гамбургер-меню** - удобная навигация на мобильных устройствах
- **Responsive формы** - оптимизированные поля ввода для виртуальной клавиатуры

### Производительность:
- **Быстрая загрузка** на медленных соединениях
- **Минимизированные ресурсы** - экономия трафика
- **Ленивая загрузка** изображений
- **Кэширование** статических ресурсов

## 📁 Структура проекта

```
src/main/java/ru/anapa/autorent/
├── config/          # Конфигурации Spring
├── controller/      # REST контроллеры
├── dto/            # Data Transfer Objects
├── entity/         # JPA сущности
├── repository/     # Репозитории данных
├── service/        # Бизнес-логика
└── util/           # Утилиты

src/main/resources/
├── static/
│   ├── css/
│   │   └── main.css        # Единый адаптивный CSS файл
│   ├── js/
│   │   └── main.js         # Единый мобильно-оптимизированный JS файл
│   └── images/
├── templates/      # Адаптивные Thymeleaf шаблоны
└── application.properties
```

## 🚀 Быстрый старт

### Предварительные требования:
- Java 17+
- Maven 3.6+
- MySQL 8.0+
- Docker (опционально)

### Установка и запуск:

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd autorent
```

2. **Настройка базы данных:**
```sql
CREATE DATABASE autorent CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'autorent'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON autorent.* TO 'autorent'@'localhost';
FLUSH PRIVILEGES;
```

3. **Настройка конфигурации:**
```properties
# application.properties
spring.datasource.url=jdbc:mysql://localhost:3306/autorent
spring.datasource.username=autorent
spring.datasource.password=password
```

4. **Запуск приложения:**
```bash
mvn spring-boot:run
```

5. **Открытие в браузере:**
```
http://localhost:8080
```

### Запуск через Docker:
```bash
docker-compose up -d
```

### Тестирование мобильной версии:
- Откройте DevTools в браузере (F12)
- Включите режим эмуляции мобильного устройства
- Выберите различные размеры экранов для тестирования

## 🔐 Роли пользователей

### ROLE_USER (Пользователь):
- Просмотр каталога автомобилей
- Бронирование автомобилей
- Управление личным кабинетом
- Просмотр истории аренд
- Оставление отзывов
- **Удобное использование на мобильных устройствах**

### ROLE_ADMIN (Администратор):
- Полный доступ к системе
- Управление пользователями
- Управление автопарком
- Просмотр статистики
- Управление арендами
- **Адаптивный интерфейс для планшетов**

## 📊 API Endpoints

### Аутентификация:
- `POST /auth/login` - вход в систему
- `POST /auth/register` - регистрация
- `POST /auth/logout` - выход из системы

### Автомобили:
- `GET /cars` - список всех автомобилей
- `GET /cars/{id}` - детали автомобиля
- `POST /cars` - добавление автомобиля (ADMIN)
- `PUT /cars/{id}` - редактирование автомобиля (ADMIN)
- `DELETE /cars/{id}` - удаление автомобиля (ADMIN)

### Аренды:
- `GET /rentals` - список аренд пользователя
- `GET /rentals/{id}` - детали аренды
- `POST /rentals` - создание новой аренды
- `PUT /rentals/{id}/cancel` - отмена аренды

### Пользователи:
- `GET /admin/users` - список пользователей (ADMIN)
- `GET /admin/users/{id}` - детали пользователя (ADMIN)
- `PUT /admin/users/{id}` - редактирование пользователя (ADMIN)

## 🔧 Конфигурация

### Основные настройки:
```properties
# База данных
spring.datasource.url=jdbc:mysql://localhost:3306/autorent
spring.datasource.username=autorent
spring.datasource.password=password

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-app-password

# JWT
jwt.secret=your-jwt-secret-key
jwt.expiration=86400000

# SMS (SMS.ru)
sms.api.key=your-sms-api-key

# Мобильная оптимизация
spring.resources.chain.strategy.content.enabled=true
spring.resources.chain.strategy.content.paths=/**
```

## 🚨 Безопасность

- **BCrypt** для хеширования паролей
- **JWT токены** для аутентификации
- **CSRF защита** для форм
- **Валидация** всех входных данных
- **SQL инъекции** предотвращены через JPA
- **XSS защита** через Thymeleaf

## 📈 Мониторинг и логирование

- **Logback** для логирования
- **Spring Boot Actuator** для мониторинга
- **Кастомные логи** для бизнес-операций
- **Обработка исключений** с детальным логированием

## 🐛 Известные проблемы и исправления

### Исправлено:
- ✅ **Ошибка в HomeController**: Добавлены недостающие зависимости (CarService, UserService, RentalService) и формирование статистики
- ✅ **Ошибка в шаблоне home.html**: Исправлены ссылки на несуществующие поля (mainImageUrl → images[0].imageUrl, dailyRate → pricePerDay)
- ✅ **Проблема с маршрутизацией**: Исправлен конфликт URL `/rentals/my-rentals` с параметризованным маршрутом
- ✅ **Унификация CSS/JS**: Все стили и скрипты объединены в единые файлы main.css и main.js
- ✅ **Мобильная адаптивность**: Полная поддержка мобильных устройств и планшетов

### Текущие ограничения:
- 🔄 SMS-уведомления требуют настройки API ключа
- 🔄 Email-уведомления требуют настройки SMTP сервера
- 🔄 Загрузка изображений требует настройки файлового хранилища

## 📱 Рекомендации по мобильному использованию

### Для пользователей:
- **Используйте последние версии браузеров** для лучшей производительности
- **Включите JavaScript** для полной функциональности
- **Используйте Wi-Fi** для быстрой загрузки изображений
- **Сохраните сайт в закладки** для быстрого доступа

### Для разработчиков:
- **Тестируйте на реальных устройствах** - эмуляция не всегда точна
- **Проверяйте производительность** на слабых устройствах
- **Оптимизируйте изображения** для мобильных сетей
- **Используйте прогрессивное улучшение** - базовая функциональность должна работать везде

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 📞 Поддержка

Если у вас есть вопросы или проблемы:
- Создайте Issue в GitHub
- Обратитесь к документации
- Проверьте логи приложения

## 🔄 Обновления

### Последние изменения:
- **v1.3.0**: Добавлена полная мобильная адаптивность и оптимизация для сенсорного управления
- **v1.2.0**: Исправлена ошибка в HomeController и шаблоне home.html
- **v1.1.0**: Унификация CSS и JS файлов
- **v1.0.0**: Первоначальный релиз

---

**AutoRent** - современное решение для аренды автомобилей с удобным интерфейсом, надежной архитектурой и полной поддержкой мобильных устройств.

## 📱 Мобильная адаптивность (v1.4.0)

### ✅ Выполненные улучшения

#### 🎨 CSS (Mobile-First подход)
- **Переписан `main.css`** с использованием Mobile-First подхода
- **Адаптивная типографика** с использованием `clamp()` для плавного масштабирования
- **Адаптивная сетка** на Flexbox/Grid вместо самописных классов
- **Оптимизация для сенсорных экранов** - минимальные размеры кнопок 44px
- **Медиа-запросы** с `min-width` вместо `max-width` для лучшей производительности

#### 📱 JavaScript
- **Мобильное меню** с гамбургер-кнопкой для навигации
- **Управление выпадающими меню** с поддержкой touch-событий
- **Плавная прокрутка** и анимации для лучшего UX

#### 🏗️ HTML шаблоны
- **Единообразие подключений** через `fragments/head.html`
- **Адаптивная сетка Bootstrap 5** во всех пользовательских шаблонах
- **Убраны дублирования** Bootstrap JS и CSS
- **Оптимизированы формы** для мобильных устройств

#### 📄 Обновленные шаблоны
- ✅ `home.html` - главная страница с адаптивной сеткой
- ✅ `auth/login.html` - форма входа
- ✅ `auth/register.html` - форма регистрации  
- ✅ `cars/list.html` - список автомобилей
- ✅ `cars/view.html` - детали автомобиля
- ✅ `rentals/my-rentals.html` - мои аренды
- ✅ `rentals/new.html` - создание аренды
- ✅ `reviews/create.html` - создание отзыва
- ✅ `about.html` - о нас
- ✅ `contact.html` - контакты

### 📱 Адаптивные брейкпоинты
- **Мобильные:** < 768px (1 колонка)
- **Планшеты:** 768px - 991px (2 колонки)  
- **Десктопы:** 992px+ (3-4 колонки)

### 🎯 Ключевые особенности
- **Touch-friendly интерфейс** с увеличенными кнопками
- **Адаптивные изображения** с правильными пропорциями
- **Оптимизированные формы** для мобильного ввода
- **Быстрая загрузка** благодаря Mobile-First подходу

---

**AutoRent** - современная система аренды автомобилей с полной мобильной поддержкой 🚗📱 

## Изменения в логике аренды (июнь 2025)

- В форме создания аренды (`/rentals/new`) поля даты и времени (`startDate`, `endDate`) теперь всегда передаются в формате `yyyy-MM-dd'T'HH:mm`, что полностью совместимо с типом `LocalDateTime` на сервере.
- Для этого в шаблоне используется:
  ```html
  <input type="datetime-local" ... th:value="${#dates.format(rental.startDate, 'yyyy-MM-dd'T'HH:mm')}" ...>
  ```
- Поле `durationDays` вычисляется на клиенте через JavaScript и передаётся как скрытое поле.
- Это гарантирует отсутствие ошибок конвертации дат и корректную валидацию на сервере. 